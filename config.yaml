gateway:
  port: 9003
  host: "0.0.0.0"
  google_client_id: ""

services:
  - name: cardhub
    url: ${CARDHUB_URL:-http://localhost:8000}
    spec: specs/cardhub/swagger.json

  # Add more services below — each needs a specs/<name>/swagger.json file
  # and an upstream URL env var. The spec file is auto-seeded on startup.
  #
  # - name: my-service
  #   url: ${MY_SERVICE_URL:-http://localhost:8001}
  #   spec: specs/my-service/swagger.json

# No users defined = open mode (no auth required, all admin endpoints accessible).
# Add users below to enable token-based access control.
# users:
#   - name: "admin"
#     token: ${MOCK_ADMIN_TOKEN:-mock-admin-secret}
#     role: "admin"
#     envs: ["uat-api", "uat-ui", "uat-dev", "prod-api", "prod-ui", "prod-dev"]
#
#   - name: "tester"
#     token: ${MOCK_TESTER_TOKEN:-mock-tester-secret}
#     role: "tester"
#     envs: ["uat-api", "uat-ui", "uat-dev", "prod-api", "prod-ui", "prod-dev"]
#
#   - name: "developer"
#     token: ${MOCK_VIEWER_TOKEN:-mock-dev-secret}
#     role: "viewer"
#     envs: ["uat-dev", "prod-dev"]
